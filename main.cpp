#include <iostream>
#include "MyVector.h"
#include <vector>

int main()
{
  /*
  std::vector<int> v{1, 2, 3};
  std::cout << v[3] << '\n';
  std::cout << "capacity = " << v.capacity() << '\n';
  v.push_back(4);
  std::cout << "capacity = " << v.capacity() << '\n';
  v.push_back(5);
  v.push_back(6);
  v.push_back(7);
  std::cout << "capacity = " << v.capacity() << '\n';
  */
}
